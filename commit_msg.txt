fix: screen-by-screen visual audit fixes

Critical:
- NavGraph: register missing LOGO_PICKER route (would crash from Profile)
- ProfileScreen: remove dead "challenge" / "Reto" mode label (mode was deleted)
- ErrorScreen + LoadingScreen: add explicit background(BgDark) (transparent before)
- ErrorScreen: use Icons.Default.ErrorOutline instead of emoji (match LoadingScreen)

Theme tokens:
- Add Silver, TrackColor, TrackColorDim, GlassSurface, Scrim, ScrimStrong, ScrimHeavy
- Replace 16 hardcoded Color.White/Black.copy(alpha) across screens with tokens

Consistency:
- GameOverScreen: chest SILVER uses new Silver token instead of TextOption
- HomeScreen + ModeSelectScreen: flat background instead of gradient with identical colors
- ExamResultScreen: remove redundant background(BgDark) under Scaffold
- Add containerColor = BgCard to 5 dialogs relying on implicit colorScheme
- LogoPickerScreen: use shared StaggeredAppearance, drop duplicate function
- HelpScreen: move 43 hardcoded Spanish strings to strings.xml

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
